# Backend API base URL. Optional — when left unset the app picks it
# automatically:
#   • local dev  → "/api" (proxied by Vite to http://localhost:8000)
#   • production → https://smart-scid-5-cv.vercel.app
# Set this only to override the default (e.g. a different backend).
VITE_API_URL=
